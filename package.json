{
  "name": "bg3-builder",
  "version": "1.0.0",
  "description": "Intended Goal <br> Make a web page that holds all class, race, feat, and equipment data that the user can change in order to make a viable build without having to google a bunch of things or find every item over the course of several runs",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build",
    "render-build": "npm install && npm run build",
    "seed": "cd server && npm run seed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/client": "^3.11.8",
    "concurrently": "^9.0.1",
    "jwt-decode": "^4.0.0"
  }
}
